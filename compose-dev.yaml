services:
  app:
    build:
      context: .
      target: development
    entrypoint:
    - sleep
    - infinity
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

